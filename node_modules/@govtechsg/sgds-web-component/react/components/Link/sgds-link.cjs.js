'use client';
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var lit = require('lit');
var decorators_js = require('lit/decorators.js');
var sgdsElement = require('../../base/sgds-element.cjs.js');
var link = require('./link.cjs.js');

/**
 * @summary Link allows users to click and navigate their way from page to page
 *
 * @slot default - Pass in a single anchor tag here
 */
class SgdsLink extends sgdsElement["default"] {
    constructor() {
        super(...arguments);
        /** Determines the size of the link */
        this.size = "md";
        /** Sets the colour of the link @deprecated since 3.6.0 */
        this.variant = "primary";
        /** Sets the colour of the link, replaces variant prop  */
        this.tone = "primary";
        /** When true, sets the active stylings of the link */
        this.active = false;
        /** Disables the link */
        this.disabled = false;
    }
    _processAnchor(anchor) {
        if (this.disabled) {
            anchor.setAttribute("disabled", "true");
        }
        if (anchor.hasAttribute("disabled")) {
            anchor.setAttribute("href", "javascript:void(0)");
            anchor.setAttribute("tabindex", "-1");
        }
        else {
            anchor.setAttribute("tabindex", "0");
        }
    }
    _processIcon(anchor) {
        const linkToIconSizeMapping = {
            xs: "sm",
            sm: "md",
            md: "lg",
            lg: "xl"
        };
        const icons = anchor.querySelectorAll("sgds-icon");
        icons.forEach(icon => {
            // icon.size = linkToIconSizeMapping[this.size]
            icon.setAttribute("size", linkToIconSizeMapping[this.size]);
            icon.classList.remove("icon-left", "icon-right");
            if (!icon.previousElementSibling && !icon.previousSibling) {
                icon.classList.add("icon-left");
            }
            if (!icon.nextElementSibling && !icon.nextSibling) {
                icon.classList.add("icon-right");
            }
        });
    }
    _handleSlotChange(e) {
        const anchor = e.target
            .assignedElements()
            .find(el => el.tagName.toLowerCase() === "a");
        if (anchor) {
            this._processAnchor(anchor);
            this._processIcon(anchor);
        }
    }
    firstUpdated(changedProperties) {
        super.firstUpdated(changedProperties);
        const anchor = this.querySelector("a");
        if (anchor) {
            this._processAnchor(anchor);
            this._processIcon(anchor);
        }
    }
    render() {
        /** When removing href, link is no longer focusable */
        return lit.html `<slot class="nav-link " @slotchange=${this._handleSlotChange}></slot> `;
    }
}
SgdsLink.styles = [...sgdsElement["default"].styles, link["default"]];
tslib.__decorate([
    decorators_js.property({ type: String, reflect: true })
], SgdsLink.prototype, "size", void 0);
tslib.__decorate([
    decorators_js.property({ type: String, reflect: true })
], SgdsLink.prototype, "variant", void 0);
tslib.__decorate([
    decorators_js.property({ type: String, reflect: true })
], SgdsLink.prototype, "tone", void 0);
tslib.__decorate([
    decorators_js.property({ type: Boolean, reflect: true })
], SgdsLink.prototype, "active", void 0);
tslib.__decorate([
    decorators_js.property({ type: Boolean, reflect: true })
], SgdsLink.prototype, "disabled", void 0);

exports.SgdsLink = SgdsLink;
exports["default"] = SgdsLink;
//# sourceMappingURL=sgds-link.cjs.js.map
