// The army and school help screens are identical in the mockups, so this re-exports
// the shared implementation. Give it its own body if the two ever diverge.
export { default } from "../school/HelpSupportPage";